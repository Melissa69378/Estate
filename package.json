{
  "name": "laforet-immobilier",
  "version": "1.0.0",
  "private": true,
  "description": "LaForêt Immobilier public site and administration API",
  "main": "server.js",
  "scripts": {
    "dev": "node server.js",
    "build": "node --check server.js && node --check script.js && node --check admin.js && node --check virtual-tour.js",
    "lint": "node --check server.js && node --check script.js && node --check admin.js && node --check virtual-tour.js",
    "start": "node server.js",
    "check": "node --check server.js && node --check script.js && node --check admin.js && node --check virtual-tour.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@photo-sphere-viewer/core": "^5.15.1",
    "@photo-sphere-viewer/markers-plugin": "^5.15.1",
    "pg": "^8.13.1",
    "three": "^0.185.1"
  }
}
